import type { TatomirPlugin } from '../../types'

export const puckPlugin: TatomirPlugin = {
  id: 'editor-puck',
  name: 'Puck Editor',
  version: '1.0.0',
  description:
    'Visual page builder powered by Puck (MIT). React-native components, drag-and-drop, JSON storage.',
}
